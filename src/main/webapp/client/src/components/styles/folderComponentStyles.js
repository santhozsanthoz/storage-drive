import { makeStyles } from "@mui/styles";

export default makeStyles((theme) =>
    ({
        folder: {
            display: "flex",
            flexDirection: "column",
            alignItems: "center",
            gap: "3px",
            margin: "10px"
        }
    })
);
