import { makeStyles } from "@mui/styles";

export default makeStyles((theme) =>
    ({
        navbar: {
            height: "35px",
            backgroundColor: "#53CBFB",
            display: "flex",
            justifyContent: "space-between",
            alignItems: "center"
        },
        navComps: {
            margin: "10px"
        }
    })
);
